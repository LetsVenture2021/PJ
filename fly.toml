app = "pj-runtime"
primary_region = "iad"

[build]

[env]
  PORT = "3001"

[processes]
  app = "python realtime_server.py"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]
